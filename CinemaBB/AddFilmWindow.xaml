<Window x:Class="CinemaBB.AddFilmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaBB"
        mc:Ignorable="d"
              Title="Добавитьпользователя" SizeToContent="WidthAndHeight"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="10" />
            <RowDefinition Height="50" />
            <RowDefinition Height="10" />
            <RowDefinition Height="50" />
            <RowDefinition Height="10" />
            <RowDefinition Height="50" />
            <RowDefinition Height="10" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Название фильма:"/>
        <TextBox x:Name="NameTb" Grid.Row="0" Grid.Column="2" 
                 Text="{Binding Name}"/>

        <TextBlock Grid.Row="2" Text="Жанр:" />
        <TextBox x:Name="GenreTb" Grid.Row="2" Grid.Column="2"
                  Text="{Binding genre}"/>

        <TextBlock Grid.Row="4" Text="время(мин):"/>
        <TextBox x:Name="timeTb" Grid.Row="4" Grid.Column="2"
                 Text="{Binding time}"/>

        <TextBlock Grid.Row="6" Text="возраст:"/>
        <TextBox x:Name="ageTb" Grid.Row="6" Grid.Column="2"
                 Text="{Binding age}"/>
        <!--ff-->
        <!--аа-->

        <Button x:Name="AddFilmBtn" Grid.Row="12" Grid.Column="2"
                 Click="AddFilmBtn_Click" Content="Добавить компанию"/>
    </Grid>

</Window>
